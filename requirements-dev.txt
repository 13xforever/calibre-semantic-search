# Dev/test dependencies for running `python dev.py` (the full gate) on a new system.
# NOT shipped: the plugin ZIP contains only src/ and runs on calibre's Python with
# the stdlib alone (lancedb/zstandard are optional at runtime there too). The version
# pins match what the external-dependency installer (DEP_PINS in src/utils.py) fetches.
ruff             # lint step: pyflakes + isort rules (config in ruff.toml)
lancedb==0.38.0  # hard test dependency - the lancedb backend tests must run, not skip
lxml==6.1.1      # exact version calibre 9.14 bundles; the chunker's HTML walk runs against it in tests too (keep in sync when calibre updates it)
numpy==2.5.3     # optional at runtime (pure-Python fallback), but needed here to exercise the numpy paths in tests; calibre does not bundle it
zstandard==0.25.0  # zstd text codec; without it a fresh store falls back to zlib
